<h1> Articles </h1>

<table>
	<tr>
		<th> Title </th>
		<th> Date </th>
	</tr>
<% @articles.each do |article| %>
	<tr>
		<th> 
		<%= link_to article.title, article_path(article.id) %></th>
		<th> Date </th>
	</tr>


<% end %>
</table>